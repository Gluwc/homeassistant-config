popup_auto:
  template: tile
  tap_action:
    action: fire-dom-event
    browser_mod:
      service: browser_mod.popup
      data:
        content: !include includes/auto.yaml
        deviceID:
          - this
        hide_header: true
  custom_fields: !include includes/custom_fields.yaml
