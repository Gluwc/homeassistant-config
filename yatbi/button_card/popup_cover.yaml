popup_cover:
  template: tile
  tap_action:
    action: fire-dom-event
    browser_mod:
      command: popup
      card: !include includes/cover.yaml
      deviceID:
        - this
      hide_header: true
  custom_fields: !include includes/custom_fields.yaml
